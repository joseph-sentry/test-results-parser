name: build and release wheels

on:
  release:
    types:
      - created

permissions:
  contents: read

jobs:
  buildwheels:
    name: Build wheels
    uses: ./.github/workflows/CI.yml
    with:
      release: true
    secrets: inherit
